services:
  paopizza-fe:
    container_name: paopizza-fe
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HOST_PORT:-3001}:3000"
    env_file:
      - .env
    restart: unless-stopped
